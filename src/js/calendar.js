import flatpickr from 'flatpickr';
const flatpickr = require('flatpickr');

flatpickr('#calendar', {
  altFormat: 'F j, Y',
  dateFormat: 'd/m/Y',
});

// const calendarHidden = document.querySelectorAll('flatpickr-calendar');
// calendarHidden.classList.add('visually-hidden');
// return calendarHidden;
